# Local backend — required when using Camera IP (phone on your Wi‑Fi).
# Cloud hosts cannot reach 192.168.x.x phone addresses.
VITE_API_URL=http://localhost:8001

# Optional face-api model path (default /models)
# VITE_FACE_MODEL_URL=/models
