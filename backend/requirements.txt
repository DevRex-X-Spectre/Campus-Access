# Campus Access API — pin versions for reproducible installs.
#
# REQUIRED: Python 3.11 or 3.12 only (not 3.13/3.14).
#
#   python3.12 -m venv .venv
#   source .venv/bin/activate
#   python -V    # must print 3.12.x
#   pip install -r requirements.txt
#
# PyTorch CPU wheels (no CUDA):
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.3
python-multipart==0.0.20
python-dotenv==1.1.0

# Vision / ML (CPU-only)
# facenet-pytorch 2.6.0 requires:
#   torch     >=2.2.0, <2.3.0
#   numpy     >=1.24,  <2.0
#   Pillow    >=10.2,  <10.3
# torchvision 0.17.2 matches torch 2.2.2
torch==2.2.2
torchvision==0.17.2
facenet-pytorch==2.6.0
numpy==1.26.4
Pillow==10.2.0
